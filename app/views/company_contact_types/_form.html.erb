<%= form_with(model: company_contact_type) do |form| %>
  <% if company_contact_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_contact_type.errors.count, "error") %> prohibited this company_contact_type from being saved:</h2>

      <ul>
        <% company_contact_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :role %>
    <%= form.text_field :role %>
  </div>

  <div class="form-group">
    <%= form.label :definition %>
    <%= form.text_field :definition %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
