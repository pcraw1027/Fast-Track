<%= form_with(model: company_gender_stat) do |form| %>
  <% if company_gender_stat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_gender_stat.errors.count, "error") %> prohibited this company_gender_stat from being saved:</h2>

      <ul>
        <% company_gender_stat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :company_id %>
    <%= form.text_field :company_id %>
  </div>

  <div class="form-group">
    <%= form.label :gender_type_id %>
    <%= form.text_field :gender_type_id %>
  </div>

  <div class="form-group">
    <%= form.label :employee_type_id %>
    <%= form.text_field :employee_type_id %>
  </div>

  <div class="form-group">
    <%= form.label :count %>
    <%= form.number_field :count %>
  </div>

  <div class="form-group">
    <%= form.label :percentage %>
    <%= form.number_field :percentage %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
