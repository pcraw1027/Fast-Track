<div class="jumbotron">
  <div class="row">
    <div class="col">

<%if @scan.product_id%>
   <p>
    <strong>Product:</strong>
    <a href="/products/<%= @scan.product_id %>"><%= @scan.product&.name %> </a>
   </p>
<%end%>

<p>
  <strong>User:</strong>

  <a href="/users/<%= @scan.user_id %>"><%= @scan.user.username %> </a>
</p>

<p>
  <strong>Scan date:</strong>
  <%= @scan.scan_date %>
</p>

<p>
  <strong>Barcode:</strong>
  <%= @scan.barcode %>
</p>

<p>
  <strong>Product exists:</strong>
  <%= @scan.product_exists %>
</p>
</div>
</div>
</div>
<%= link_to 'Edit', edit_scan_path(@scan) %> |
<%= link_to 'Back', scans_path %>
