<%= form_with(model: employee_type) do |form| %>
  <% if employee_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee_type.errors.count, "error") %> prohibited this employee_type from being saved:</h2>

      <ul>
        <% employee_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :employee %>
    <%= form.text_field :employee %>
  </div>

  <div class="form-group">
    <%= form.label :definition %>
    <%= form.text_field :definition %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
