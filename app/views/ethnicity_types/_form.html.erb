<%= form_with(model: ethnicity_type) do |form| %>
  <% if ethnicity_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ethnicity_type.errors.count, "error") %> prohibited this ethnicity_type from being saved:</h2>

      <ul>
        <% ethnicity_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :ethnicity %>
    <%= form.text_field :ethnicity %>
  </div>

  <div class="form-group">
    <%= form.label :definition %>
    <%= form.text_field :definition %>
  </div>

  <div class="form-group">
    <%= form.label :source %>
    <%= form.text_field :source %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
