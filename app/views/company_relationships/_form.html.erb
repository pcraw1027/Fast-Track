<%= form_with(model: company_relationship) do |form| %>
  <% if company_relationship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_relationship.errors.count, "error") %> prohibited this company_relationship from being saved:</h2>

      <ul>
        <% company_relationship.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :company_relationship_type_id %>
    <%= form.collection_select :company_relationship_type_id, @company_relationship_types, :id, :relationship, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :parent_company_id %>
    <%= form.collection_select :parent_company_id, @parent_companies, :id, :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :child_company_id %>
    <%= form.collection_select :child_company_id, @child_companies, :id, :name, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "Submit", class: 'action-btn' %>
  </div>
<% end %>
