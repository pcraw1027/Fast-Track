<div class="row">
    <div class = "col-md-12">
      <%= form_with url: domains_companies_company_locations_import_path(company_id: company.id), multipart: true, local: true do |f| %>

        <%= f.hidden_field :mid, value: cit_record.mid %>
        <%= f.hidden_field :filter_by, value: params[:filter_by] %>
        <%= f.hidden_field :level, value: params[:level] %>

        <div class="row">
            <div class="form-group mb-3">
              <%= f.label :file, "Upload store addresses in CSV" %>
              <%= f.file_field :file, accept: ".csv", required: true %>
            </div>

            <div class="form-group mb-3">
              <%= f.submit "Import Addresses", class: "btn btn-primary" %>
            </div>
            
        </div>

    <% end %>
    </div>
</div>



<%= form_for(company, url: domains_companies_company_update_to_level_two_path(company_id: company.id), html: { method: :put }) do |form| %>

  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
        <% company.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :cit_record_id, value: cit_record.id %>
  <%= form.hidden_field :mid, value: cit_record.mid %>
  <%= form.hidden_field :filter_by, value: params[:filter_by] %>
  <%= form.hidden_field :level, value: params[:level] %>


  <div class="row">
    <div class = "col-md-12">

      <div class="container mt-5">

            <div id="addresses">
              <%= form.fields_for :addresses do |address_form| %>
                
                      <%= render 'address_fields', f: address_form  %>
                 <hr>
              <% end %>
            </div>
      </div>
    </div>
  </div>

<div class="row">
    <div class = "col-md-12">
      <i class="fas fa-plus" style="color: #0B9444;" ></i> <%= link_to_add_association 'add address', form, :addresses, class:"theme-link" %>
    </div>
  </div>
</div>


<div class="actions text-center">
    <%= form.submit "Update", class: 'action-btn' %>
  </div>
<% end %>

