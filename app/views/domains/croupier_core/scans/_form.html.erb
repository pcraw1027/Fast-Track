<%= form_with(model: scan) do |form| %>
  <% if scan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scan.errors.count, "error") %> prohibited this scan from being saved:</h2>

      <ul>
        <% scan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :scan_date %>
    <%= form.datetime_select :scan_date %>
  </div>

  <div class="field">
    <%= form.label :barcode %>
    <%= form.text_field :barcode %>
  </div>

  <div class="field">
    <%= form.label :product_exists %>
    <%= form.check_box :product_exists %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
