<%= form_with(model: industry_category_type_mapping) do |form| %>
  <% if industry_category_type_mapping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(industry_category_type_mapping.errors.count, "error") %> prohibited this industry_category_type_mapping from being saved:</h2>

      <ul>
        <% industry_category_type_mapping.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field form-group">
  <%= form.label :mapping_type %>
  <%= form.select :mapping_type, 
        Domains::Companies::IndustryCategoryTypeMapping.mapping_types.keys.map { |k| [k.titleize, k] }, 
        { prompt: "Select mapping type" }, 
        class: "form-control" %>
</div>

<div class="field form-group">
  <%= form.label :category_code_type_from_id, "Category Code Type (From)" %>
  <%= form.text_field :category_code_type_from_id, class: "form-control" %>
</div>


<div class="field form-group">
  <%= form.label :category_code_type_to_id, "Category Code Type (To)" %>
  <%= form.text_field :category_code_type_to_id, class: "form-control" %>
</div>


  <div class="actions">
    <%= form.submit "Submit", class: 'action-btn' %>
  </div>
<% end %>
