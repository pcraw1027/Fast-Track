<%= form_with(model: list) do |form| %>
  <% if list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
        <% list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :list_type %>
    <%= form.number_field :list_type %>
  </div>

  <div class="field">
    <%= form.label :default %>
    <%= form.text_field :default %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :snapshot_aggregate %>
    <%= form.text_field :snapshot_aggregate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
