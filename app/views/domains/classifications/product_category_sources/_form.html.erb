<%= form_with(model: product_category_source) do |form| %>
  <% if product_category_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_category_source.errors.count, "error") %> prohibited this product_category_source from being saved:</h2>

      <ul>
        <% product_category_source.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="form-group">
    <%= form.label :description %>
    <%= form.text_area :description, class: "form-control", rows: 5 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
